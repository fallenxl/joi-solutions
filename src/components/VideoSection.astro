 <section id="ofrecemos" class="relative py-20 bg-white text-center">
    <div class="max-w-3xl mx-auto px-6">
      <h2 class="text-3xl md:text-4xl font-medium text-neutral-700 mb-4">
        Innovación útil que mejora experiencias reales
      </h2>
      <p class="text-gray-600 text-lg">
        En Joi Solutions, rediseñamos la forma en que se paga y se gestiona.
        Nuestra visión transforma cada punto de contacto en una experiencia
        ágil, segura y humana.
      </p>

      <!-- Botón de video -->
      <div class="relative mt-10 flex justify-center">
        <button
          id="videoTrigger"
          class="relative w-20 h-20 rounded-full bg-tertiary/80 hover:bg-tertiary transition-colors flex items-center justify-center shadow-lg group"
        >
          <!-- Pulso animado -->
          <span
            class="absolute inline-flex h-full w-full rounded-full bg-tertiary opacity-75 animate-ping"
          ></span>
          <!-- Ícono -->
          <svg
            class="w-8 h-8 text-white relative z-10"
            fill="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              d="M10 8.64L15 12L10 15.36V8.64ZM12 2C6.48 2 2 6.48 2 12s4.48 10 10 10
             10-4.48 10-10S17.52 2 12 2z"
            ></path>
          </svg>
        </button>
      </div>
    </div>

    <!-- Modal de video -->
    <div
      id="videoModal"
      class="fixed inset-0 bg-black/70 backdrop-blur-sm flex items-center justify-center z-[60] hidden"
    >
      <div class="relative w-[90%] max-w-2xl aspect-video">
        <button
          id="closeModal"
          class="absolute -top-4 -right-4 bg-white text-gray-700 rounded-full p-2 shadow-md hover:bg-gray-100 z-50"
        >
          <svg
            class="w-5 h-5"
            viewBox="0 0 24 24"
            stroke="currentColor"
            fill="none"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
        <iframe
          id="videoIframe"
          class="w-full h-full rounded-lg"
          src=""
          title="Closeloop Video"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen></iframe>
      </div>
    </div>


  </section>
      <script type="module">
      const trigger = document.getElementById("videoTrigger");
      const modal = document.getElementById("videoModal");
      const close = document.getElementById("closeModal");
      const iframe = document.getElementById("videoIframe");

      const videoURL = "https://www.youtube.com/embed/IlaiSFtf7zY?autoplay=1";

      trigger.addEventListener("click", () => {
        modal.classList.remove("hidden");
        iframe.src = videoURL;
      });

      close.addEventListener("click", () => {
        modal.classList.add("hidden");
        iframe.src = "";
      });
    </script>